# dependencies
node_modules/

# build output
dist/
dist-ssr/

# env (secrets)
.env.local
.env*.local

# logs
*.log
npm-debug.log*

# editor / os
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# misc
*.local
.cache/

# generated printable QR codes (regenerate with `npm run qr`)
qr/

# raw source art (large originals; processed outputs ship in public/assets).
# Regenerate with `npm run assets` / `npm run logos` after editing these.
NewAssets/
LOGO/

# raw font source (full Montserrat family; the 4 used weights ship in public/fonts).
Montserrat/
